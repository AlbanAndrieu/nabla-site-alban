---
description: Git and GitHub rules for this repository
globs: *.git*
alwaysApply: true
---

# Version control

Follow `/AGENTS.md` as the canonical policy.

- `master` is the default branch and must be treated as protected by agent policy. Never write, push, merge-agent changes or move its ref directly.
- Create/reuse a non-default branch, validate it, then integrate through a pull request targeting `master`.
- After edits, run the self-converging `npm run quality:agent:fix`, commit the result, then push normally so the installed pre-push hook runs the strict publication gate once.
- `QG_AUTOFIX_REQUIRED` means rerun the local fix path; do not spend context on broad CI-log analysis unless the failure is non-auto-fixable.
- Use Conventional Commits and the rules in `commitlint.config.ts`.
- OpenCommit and `.opencommit-commitlint` are intentionally retained as local/on-demand commit-message tooling; Conventional Commits remain authoritative.
- Never bypass hooks or quality gates with `--no-verify`.
- Do not use an obsolete `main/develop` Git Flow model for this repository.
