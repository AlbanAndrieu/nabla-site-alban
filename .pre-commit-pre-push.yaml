minimum_pre_commit_version: 4.2.0
repos:
  - repo: local
    hooks:
      - id: quality-gate-pre-push
        name: "✅ quality · Run repository quality gate before push"
        language: system
        entry: bash scripts/quality-gate.sh
        pass_filenames: false
        always_run: true
        stages: [pre-push]
